apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "calendso-chart.fullname" . }}-test-connection"
  labels:
    {{- include "calendso-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "calendso-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
