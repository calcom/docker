apiVersion: v1
kind: Service
metadata:
  name: {{ include "calendso-chart.fullname" . }}
  labels:
    {{- include "calendso-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "calendso-chart.selectorLabels" . | nindent 4 }}
